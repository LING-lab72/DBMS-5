#include "ExprEvaluator.h"

bool ExprEvaluator::evaluate(const WhereExpr& /*expr*/,
                              const std::map<std::string, FieldValue>& /*row*/) {
    // TODO(成员E): 实现 WHERE 条件递归求值
    return true;
}
