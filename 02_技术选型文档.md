# 数据库管理系统（DBMS）技术选型文档

**版本**：3.0  
**日期**：2026-04-19

---

## 1. 总体架构

采用 **纯 C++ 原生 CLI** 架构，通过命令行交互式界面（REPL）驱动自制 DBMS 引擎。  
**不依赖任何外部数据库或第三方库**，所有数据由自制存储引擎管理，数据文件存储于本地磁盘。  
程序编译为单一可执行文件，直接运行即可。

```
用户输入（stdin / 脚本文件）
         │
         ▼
┌─────────────────────┐
│     CLI REPL 层      │  ← C++ getline/readline / ANSI 彩色 / 表格渲染 / 元命令
└──────────┬──────────┘
           │ SQL 字符串
           ▼
┌──────────────────────────────────────┐
│            自制 DBMS 引擎             │
│  Lexer ──► Parser ──► AST            │
│                  │                   │
│             Executor                 │
│                  │                   │
│          StorageEngine               │
└──────────────────┬───────────────────┘
                   │ 文件 I/O (fstream / 二进制)
           ┌───────▼───────┐
           │  本地数据文件  │
           │  .mdb  .tdf   │
           │  .trd  .tid   │
           └───────────────┘
```

**选择理由**：
- 纯 C++ 标准库，零外部依赖，编译后直接 `./dbms` 运行
- `std::fstream` 提供高效二进制文件 I/O，直接控制字节布局
- ANSI 转义序列实现彩色终端输出，无需第三方库
- 便于脚本化测试：`./dbms < test.sql` 直接批量执行
- 锻炼底层系统编程能力，与课程目标高度契合

---

## 2. 技术栈明细

### 2.1 CLI 交互层（C++ 标准库）

| 技术 | 来源 | 用途 |
|---|---|---|
| `std::cin` / `std::getline` | C++ 标准库 | 交互式 REPL 输入、多行支持 |
| ANSI 转义序列 | 内置（无依赖） | 终端彩色输出（错误红色、成功绿色、提示黄色） |
| 自制 TablePrinter | 项目内实现 | 终端表格渲染（查询结果格式化输出） |

### 2.2 自制引擎（C++17）

| 技术 | 版本 | 用途 |
|---|---|---|
| C++ 标准库 `<fstream>` | C++17 | 二进制文件读写（.mdb / .tdf / .trd / .tid） |
| `<string>` / `<vector>` / `<map>` / `<variant>` | C++17 | 数据结构与类型 |
| `<filesystem>` | C++17 | 目录创建、文件路径管理 |
| `<chrono>` | C++17 | 执行时间计量 |

> ⚠️ **不使用任何外部数据库驱动**（无 sqlite3 / leveldb 等）。  
> 所有数据读写由自制 StorageEngine 模块通过 C++ `std::fstream` 完成。

### 2.3 自制 DBMS 引擎架构

```
SQL 字符串
   │
   ▼
┌──────────┐   vector<Token>  ┌──────────┐   ASTNode*  ┌───────────┐
│  Lexer   │ ───────────────► │  Parser  │ ──────────► │ Executor  │
└──────────┘                  └──────────┘             └─────┬─────┘
                                                             │
                                            ┌────────────────▼───────────────┐
                                            │         StorageEngine           │
                                            │   DatabaseManager               │
                                            │   TableManager                  │
                                            │   RecordManager                 │
                                            │   IndexManager (B+Tree)         │
                                            └────────────────┬───────────────┘
                                                             │ fstream 二进制 I/O
                                                   本地 .mdb/.tdf/.trd/.tid 文件
```

```cpp
// src/engine/DBEngine.h —— 引擎对外统一入口
class DBEngine {
public:
    QueryResult execute(const std::string& sql, Session& session);
    void beginTransaction(Session& session);
    void commit(Session& session);
    void rollback(Session& session);
};

// 文件格式约定
// .mdb  —— 系统目录文件（数据库列表、元信息）
// .tdf  —— 表定义文件（字段名、类型、约束）
// .trd  —— 表记录文件（定长二进制记录）
// .tid  —— 索引文件（B+Tree 节点页）
```

### 2.4 测试

| 技术 | 用途 |
|---|---|
| 自制轻量测试框架（assert 宏） | 单元测试（引擎各模块，无外部依赖） |
| 自定义 SQL 脚本 | 集成测试（批处理模式执行）|

---

## 3. 开发工具链

| 工具 | 说明 |
|---|---|
| C++17 编译器 | g++ 11+ / clang++ 13+ / MSVC 2022 |
| CMake | 3.15+，跨平台构建系统 |
| Git + GitHub | 版本控制 |
| GitHub Projects | 任务追踪 |
| VS Code / CLion | 推荐 IDE |

---

## 4. 目录结构

```
dbms-app/
├── CMakeLists.txt
├── src/
│   ├── main.cpp                          # 程序入口
│   ├── cli/                              # CLI 交互层
│   │   ├── Repl.h / Repl.cpp            # REPL 主循环（getline 驱动）
│   │   ├── Formatter.h / Formatter.cpp  # 结果格式化（表格、行数、耗时）
│   │   ├── MetaCommands.h / .cpp        # 元命令处理（\help \quit \desc 等）
│   │   └── Session.h                    # 会话状态（当前库、事务状态）
│   ├── engine/                           # 自制 DBMS 引擎核心
│   │   ├── lexer/
│   │   │   └── Lexer.h / Lexer.cpp      # 词法分析器（Token 定义 + 分词）
│   │   ├── parser/
│   │   │   ├── Parser.h / Parser.cpp    # 语法分析器（递归下降）
│   │   │   └── AST.h                    # AST 节点类型定义
│   │   ├── executor/
│   │   │   ├── Executor.h / .cpp        # 查询执行器（调度各 Manager）
│   │   │   └── ExprEvaluator.h / .cpp  # WHERE 条件求值
│   │   ├── storage/
│   │   │   ├── DatabaseManager.h / .cpp # .mdb 系统目录读写
│   │   │   ├── TableManager.h / .cpp    # .tdf 表定义文件读写
│   │   │   ├── RecordManager.h / .cpp   # .trd 记录文件读写
│   │   │   └── IndexManager.h / .cpp    # .tid B+Tree 索引
│   │   └── DBEngine.h / DBEngine.cpp    # 引擎对外统一入口
│   └── types.h                          # 共享类型（AST 节点、QueryResult、错误码）
├── data/                                 # 默认数据文件目录（运行时生成）
├── tests/                                # 单元测试 & 集成测试 SQL 脚本
└── README.md
```

---

## 5. 技术选型对比（备选方案）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **纯 C++ 原生**（选定） | 零外部依赖、直接控制内存与文件布局、编译产物即可运行 | 开发效率低于脚本语言 | ✅ 选定 |
| Node.js + TypeScript | 开发效率高、内置 readline | 不符合"原生 C++"要求 | ❌ |
| Java | 跨平台、I/O 性能好 | 非 C++，依赖 JVM | ❌ |
| Python | 快速开发、语法简洁 | 非 C++，性能弱 | ❌ |

---

## 6. 自制 SQL 子集范围

本项目实现以下 SQL 语法子集（类 MySQL 语法）：

| 类别 | 支持语句 |
|---|---|
| DDL | `CREATE DATABASE` / `DROP DATABASE` / `SHOW DATABASES` / `USE` |
| DDL | `CREATE TABLE` / `DROP TABLE` / `SHOW TABLES` / `DESCRIBE` |
| DDL | `ALTER TABLE ADD/MODIFY/DROP COLUMN` |
| DML | `INSERT INTO` / `SELECT` / `UPDATE` / `DELETE` |
| 查询 | `WHERE`（比较 + AND/OR）/ `ORDER BY` / `LIMIT` |
| 聚合 | `COUNT` / `SUM` / `MAX` / `MIN` / `AVG` |
| 约束 | `PRIMARY KEY` / `NOT NULL` / `UNIQUE` / `DEFAULT` / `FOREIGN KEY` |
| 索引 | `CREATE INDEX` / `DROP INDEX` |
| 事务 | `BEGIN` / `COMMIT` / `ROLLBACK` |
| 安全 | `CREATE USER` / `DROP USER` / `GRANT` / `REVOKE` |

## 7. CLI 元命令列表

| 元命令 | 说明 |
|---|---|
| `\help` | 显示帮助信息 |
| `\quit` / `\q` | 退出程序 |
| `\status` | 显示当前连接状态（当前库、事务状态） |
| `\desc <table>` | 快捷查看表结构 |
| `\use <db>` | 切换数据库 |
| `\databases` | 列出所有数据库 |
| `\tables` | 列出当前库所有表 |
| `\history` | 显示历史命令 |
| `source <file>` | 执行 SQL 脚本文件 |
