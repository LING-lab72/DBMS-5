# 数据库管理系统（DBMS）技术选型文档

**版本**：1.0  
**日期**：2026-04-16

---

## 1. 总体架构

采用 **Electron + Vue 3（前端 UI）+ Node.js（自制 DBMS 引擎）** 的一体化桌面架构。  
**不依赖任何外部数据库**，所有数据由自制存储引擎管理，数据文件存储于本地磁盘。

```
┌────────────────────────────────────────────────────────┐
│                     Electron App                       │
│  ┌───────────────────┐  ┌─────────────────────────┐    │
│  │   Vue 3 渲染进程   │  │    Node.js 主进程         │    │
│  │   (UI / 组件层)   │◄─►│  (IPC + 服务层)          │    │
│  └───────────────────┘  └──────────┬──────────────┘    │
└─────────────────────────────────────┼──────────────────┘
                                      │
                     ┌────────────────▼────────────────┐
                     │          自制 DBMS 引擎           │
                     │  Lexer ──► Parser ──► AST        │
                     │                 │                │
                     │            Executor              │
                     │                 │                │
                     │          StorageEngine           │
                     └────────────────┬────────────────┘
                                      │ 文件 I/O
                              ┌───────▼───────┐
                              │  本地数据文件  │
                              │  .mdb  .tdf   │
                              │  .trd  .tid   │
                              └───────────────┘
```

**选择理由**：
- Electron 支持打包为 Windows 桌面安装包，满足需求书"Windows桌面窗口程序"要求
- Vue 3 + Vite 开发体验好，组件生态丰富
- Node.js 主进程可直接进行二进制文件 I/O，无需安装外部数据库
- IPC 通信天然隔离 UI 层与引擎层，便于单独测试

---

## 2. 技术栈明细

### 2.1 前端（渲染进程）

| 技术 | 版本 | 用途 |
|---|---|---|
| Vue 3 | ^3.4 | 组件框架 |
| Vite | ^5.x | 构建工具 |
| Pinia | ^2.x | 状态管理 |
| Vue Router | ^4.x | 页面路由 |
| Naive UI | ^2.x | UI 组件库（表格、表单、树形、Modal） |
| Monaco Editor | ^0.45 | SQL 编辑器（VS Code 同款，支持语法高亮） |
| @vueuse/core | latest | 组合式工具函数 |

### 2.2 主进程 / 自制引擎（Node.js）

| 技术 | 版本 | 用途 |
|---|---|---|
| Electron | ^29.x | 桌面容器 |
| electron-vite | ^2.x | 开发构建工具链 |
| electron-store | ^8.x | 本地配置持久化（连接历史、偏好设置） |

> ⚠️ **不使用任何外部数据库驱动**（无 mysql2 / knex / sqlite3）。  
> 所有数据读写由自制 StorageEngine 模块通过 Node.js `fs` / `Buffer` API 完成。

### 2.3 自制 DBMS 引擎架构

```
SQL 字符串
   │
   ▼
┌──────────┐    Token[]   ┌──────────┐    AST   ┌───────────┐
│  Lexer   │ ──────────► │  Parser  │ ────────► │ Executor  │
└──────────┘             └──────────┘           └─────┬─────┘
                                                       │
                                          ┌────────────▼────────────┐
                                          │     StorageEngine        │
                                          │  DatabaseManager         │
                                          │  TableManager            │
                                          │  RecordManager           │
                                          │  IndexManager (B+Tree)   │
                                          └────────────┬────────────┘
                                                       │ Buffer / fs
                                                  本地 .mdb/.tdf/.trd/.tid 文件
```

```typescript
// src/main/engine/DBEngine.ts —— 引擎对外统一入口
export interface DBEngine {
  execute(sql: string, sessionId: string): Promise<QueryResult>
  beginTransaction(sessionId: string): Promise<void>
  commit(sessionId: string): Promise<void>
  rollback(sessionId: string): Promise<void>
}

// 文件格式约定
// .mdb  —— 系统目录文件（数据库列表、元信息）
// .tdf  —— 表定义文件（字段名、类型、约束）
// .trd  —— 表记录文件（定长二进制记录）
// .tid  —— 索引文件（B+Tree 节点页）
```

### 2.4 测试

| 技术 | 用途 |
|---|---|
| Vitest | 单元测试 |
| @vue/test-utils | Vue 组件测试 |
| Playwright | E2E / 集成测试 |

---

## 3. 开发工具链

| 工具 | 说明 |
|---|---|
| Node.js | v20 LTS |
| pnpm | 包管理器 |
| ESLint + Prettier | 代码风格统一 |
| Git + GitHub | 版本控制 |
| GitHub Projects | 任务追踪 |
| VS Code | 推荐 IDE |

---

## 4. 目录结构

```
dbms-app/
├── src/
│   ├── main/                        # Electron 主进程 (Node.js)
│   │   ├── engine/                  # 自制 DBMS 引擎核心
│   │   │   ├── lexer/
│   │   │   │   └── Lexer.ts         # 词法分析器（Token 定义 + 分词）
│   │   │   ├── parser/
│   │   │   │   ├── Parser.ts        # 语法分析器（递归下降）
│   │   │   │   └── ast.ts           # AST 节点类型定义
│   │   │   ├── executor/
│   │   │   │   ├── Executor.ts      # 查询执行器（调度各 Manager）
│   │   │   │   └── ExprEvaluator.ts # WHERE 条件求值
│   │   │   ├── storage/
│   │   │   │   ├── DatabaseManager.ts  # .mdb 系统目录读写
│   │   │   │   ├── TableManager.ts     # .tdf 表定义文件读写
│   │   │   │   ├── RecordManager.ts    # .trd 记录文件读写
│   │   │   │   └── IndexManager.ts     # .tid B+Tree 索引
│   │   │   └── DBEngine.ts          # 引擎对外统一入口
│   │   ├── service/                 # 服务层（封装引擎调用，供 IPC 使用）
│   │   │   ├── DatabaseService.ts
│   │   │   ├── TableService.ts
│   │   │   ├── DataService.ts
│   │   │   ├── IndexService.ts
│   │   │   ├── SecurityService.ts
│   │   │   └── BackupService.ts
│   │   ├── ipc/                     # IPC 通道注册
│   │   └── index.ts
│   ├── renderer/                    # Vue 3 渲染进程
│   │   ├── components/
│   │   │   ├── layout/              # 主布局（侧边栏、顶栏、底栏）
│   │   │   ├── tree/                # 数据库/表导航树
│   │   │   ├── editor/              # SQL 编辑器
│   │   │   ├── result/              # 查询结果表格
│   │   │   └── dialogs/             # 各操作弹窗
│   │   ├── views/
│   │   ├── stores/                  # Pinia stores
│   │   ├── api/                     # IPC 调用封装 (renderer → main)
│   │   └── main.ts
│   └── shared/                      # 主/渲染进程共享类型
│       └── types.ts                 # AST 节点、IPC 响应、字段类型枚举
├── data/                            # 默认数据文件目录（运行时生成）
├── electron.vite.config.ts
├── package.json
└── README.md
```

---

## 5. 技术选型对比（备选方案）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Electron + Vue 3**（选定） | 现代UI、跨平台、团队熟悉、生态丰富、主进程可直接做文件 I/O | 包体积较大 | ✅ 选定 |
| Java + Swing/JavaFX | 跨平台、I/O 性能好 | UI 开发效率低、风格老旧 | ❌ |
| Python + PyQt5 | 快速开发 | 打包复杂、并发性差、二进制 I/O 不直观 | ❌ |
| C# WPF | Windows 原生、性能好 | 仅 Windows、团队学习成本高 | ❌ |

---

## 6. 自制 SQL 子集范围

本项目实现以下 SQL 语法子集（类 MySQL 语法）：

| 类别 | 支持语句 |
|---|---|
| DDL | `CREATE DATABASE` / `DROP DATABASE` / `SHOW DATABASES` / `USE` |
| DDL | `CREATE TABLE` / `DROP TABLE` / `SHOW TABLES` / `DESCRIBE` |
| DDL | `ALTER TABLE ADD/MODIFY/DROP COLUMN` |
| DML | `INSERT INTO` / `SELECT` / `UPDATE` / `DELETE` |
| 查询 | `WHERE`（比较 + AND/OR）/ `ORDER BY` / `LIMIT` |
| 聚合 | `COUNT` / `SUM` / `MAX` / `MIN` / `AVG` |
| 约束 | `PRIMARY KEY` / `NOT NULL` / `UNIQUE` / `DEFAULT` / `FOREIGN KEY` |
| 索引 | `CREATE INDEX` / `DROP INDEX` |
| 事务 | `BEGIN` / `COMMIT` / `ROLLBACK` |
| 安全 | `CREATE USER` / `DROP USER` / `GRANT` / `REVOKE` |
