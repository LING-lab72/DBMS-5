# 数据库管理系统（DBMS）技术选型文档

**版本**：2.0  
**日期**：2026-04-19

---

## 1. 总体架构

采用 **纯 Node.js CLI** 架构，通过命令行交互式界面（REPL）驱动自制 DBMS 引擎。  
**不依赖任何外部数据库**，所有数据由自制存储引擎管理，数据文件存储于本地磁盘。

```
用户输入（stdin / 脚本文件）
         │
         ▼
┌─────────────────────┐
│     CLI REPL 层      │  ← readline / 提示符 / 输出格式化 / 元命令
└──────────┬──────────┘
           │ SQL 字符串
           ▼
┌──────────────────────────────────────┐
│            自制 DBMS 引擎             │
│  Lexer ──► Parser ──► AST            │
│                  │                   │
│             Executor                 │
│                  │                   │
│          StorageEngine               │
└──────────────────┬───────────────────┘
                   │ 文件 I/O (fs / Buffer)
           ┌───────▼───────┐
           │  本地数据文件  │
           │  .mdb  .tdf   │
           │  .trd  .tid   │
           └───────────────┘
```

**选择理由**：
- 纯 Node.js 无额外依赖，直接 `node src/cli.js` 即可运行，环境简单
- readline 模块（内置）提供多行输入、历史记录等 REPL 能力
- 无 UI 框架开销，全力聚焦引擎核心逻辑
- 便于脚本化测试：`node src/cli.js < test.sql` 直接批量执行

---

## 2. 技术栈明细

### 2.1 CLI 交互层（Node.js 内置）

| 技术 | 版本 | 用途 |
|---|---|---|
| Node.js readline | 内置 | 交互式 REPL 输入、历史记录、多行支持 |
| chalk | ^5.x | 终端彩色输出（错误红色、成功绿色、提示黄色） |
| cli-table3 | ^0.6 | 终端表格渲染（查询结果格式化输出） |

### 2.2 自制引擎（Node.js）

| 技术 | 版本 | 用途 |
|---|---|---|
| Node.js fs / Buffer | 内置 | 二进制文件读写（.mdb / .tdf / .trd / .tid） |
| TypeScript | ^5.x | 类型安全，严格模式 |
| tsx | ^4.x | 开发时直接运行 TypeScript 文件 |

> ⚠️ **不使用任何外部数据库驱动**（无 mysql2 / knex / sqlite3）。  
> 所有数据读写由自制 StorageEngine 模块通过 Node.js `fs` / `Buffer` API 完成。

### 2.3 自制 DBMS 引擎架构

```
SQL 字符串
   │
   ▼
┌──────────┐    Token[]   ┌──────────┐    AST   ┌───────────┐
│  Lexer   │ ──────────► │  Parser  │ ────────► │ Executor  │
└──────────┘             └──────────┘           └─────┬─────┘
                                                       │
                                          ┌────────────▼────────────┐
                                          │     StorageEngine        │
                                          │  DatabaseManager         │
                                          │  TableManager            │
                                          │  RecordManager           │
                                          │  IndexManager (B+Tree)   │
                                          └────────────┬────────────┘
                                                       │ Buffer / fs
                                                  本地 .mdb/.tdf/.trd/.tid 文件
```

```typescript
// src/engine/DBEngine.ts —— 引擎对外统一入口
export interface DBEngine {
  execute(sql: string, sessionId: string): Promise<QueryResult>
  beginTransaction(sessionId: string): Promise<void>
  commit(sessionId: string): Promise<void>
  rollback(sessionId: string): Promise<void>
}

// 文件格式约定
// .mdb  —— 系统目录文件（数据库列表、元信息）
// .tdf  —— 表定义文件（字段名、类型、约束）
// .trd  —— 表记录文件（定长二进制记录）
// .tid  —— 索引文件（B+Tree 节点页）
```

### 2.4 测试

| 技术 | 用途 |
|---|---|
| Vitest | 单元测试（引擎各模块） |
| 自定义 SQL 脚本 | 集成测试（批处理模式执行）|

---

## 3. 开发工具链

| 工具 | 说明 |
|---|---|
| Node.js | v20 LTS |
| pnpm | 包管理器 |
| TypeScript | 严格模式（`strict: true`） |
| ESLint + Prettier | 代码风格统一 |
| Git + GitHub | 版本控制 |
| GitHub Projects | 任务追踪 |
| VS Code | 推荐 IDE |

---

## 4. 目录结构

```
dbms-app/
├── src/
│   ├── cli/                         # CLI 交互层
│   │   ├── repl.ts                  # REPL 主循环（readline 驱动）
│   │   ├── formatter.ts             # 结果格式化（表格、行数、耗时）
│   │   ├── metaCommands.ts          # 元命令处理（\help \quit \desc 等）
│   │   └── session.ts               # 会话状态（当前库、事务状态）
│   ├── engine/                      # 自制 DBMS 引擎核心
│   │   ├── lexer/
│   │   │   └── Lexer.ts             # 词法分析器（Token 定义 + 分词）
│   │   ├── parser/
│   │   │   ├── Parser.ts            # 语法分析器（递归下降）
│   │   │   └── ast.ts               # AST 节点类型定义
│   │   ├── executor/
│   │   │   ├── Executor.ts          # 查询执行器（调度各 Manager）
│   │   │   └── ExprEvaluator.ts     # WHERE 条件求值
│   │   ├── storage/
│   │   │   ├── DatabaseManager.ts   # .mdb 系统目录读写
│   │   │   ├── TableManager.ts      # .tdf 表定义文件读写
│   │   │   ├── RecordManager.ts     # .trd 记录文件读写
│   │   │   └── IndexManager.ts      # .tid B+Tree 索引
│   │   └── DBEngine.ts              # 引擎对外统一入口
│   └── types.ts                     # 共享类型（AST 节点、QueryResult、错误码）
├── data/                            # 默认数据文件目录（运行时生成）
├── tests/                           # 单元测试 & 集成测试 SQL 脚本
├── tsconfig.json
├── package.json
└── README.md
```

---

## 5. 技术选型对比（备选方案）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Node.js CLI**（选定） | 零额外依赖、环境简单、直接文件 I/O、易于脚本化测试 | 无图形界面 | ✅ 选定 |
| Electron + Vue 3 | 现代 GUI、组件丰富 | 包体积大、搭建成本高、与引擎核心无关 | ❌ |
| Java + Swing/JavaFX | 跨平台、I/O 性能好 | UI 开发效率低、风格老旧 | ❌ |
| Python + Click | 快速 CLI 开发 | 二进制 I/O 不直观、并发性差 | ❌ |

---

## 6. 自制 SQL 子集范围

本项目实现以下 SQL 语法子集（类 MySQL 语法）：

| 类别 | 支持语句 |
|---|---|
| DDL | `CREATE DATABASE` / `DROP DATABASE` / `SHOW DATABASES` / `USE` |
| DDL | `CREATE TABLE` / `DROP TABLE` / `SHOW TABLES` / `DESCRIBE` |
| DDL | `ALTER TABLE ADD/MODIFY/DROP COLUMN` |
| DML | `INSERT INTO` / `SELECT` / `UPDATE` / `DELETE` |
| 查询 | `WHERE`（比较 + AND/OR）/ `ORDER BY` / `LIMIT` |
| 聚合 | `COUNT` / `SUM` / `MAX` / `MIN` / `AVG` |
| 约束 | `PRIMARY KEY` / `NOT NULL` / `UNIQUE` / `DEFAULT` / `FOREIGN KEY` |
| 索引 | `CREATE INDEX` / `DROP INDEX` |
| 事务 | `BEGIN` / `COMMIT` / `ROLLBACK` |
| 安全 | `CREATE USER` / `DROP USER` / `GRANT` / `REVOKE` |

## 7. CLI 元命令列表

| 元命令 | 说明 |
|---|---|
| `\help` | 显示帮助信息 |
| `\quit` / `\q` | 退出程序 |
| `\status` | 显示当前连接状态（当前库、事务状态） |
| `\desc <table>` | 快捷查看表结构 |
| `\use <db>` | 切换数据库 |
| `\databases` | 列出所有数据库 |
| `\tables` | 列出当前库所有表 |
| `\history` | 显示历史命令 |
| `source <file>` | 执行 SQL 脚本文件 |
