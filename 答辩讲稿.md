# DBMS 中期答辩讲稿

> 当前 commit：`3885b13` — feat: implement core SQL engine (DDL/DML + REPL)  
> 答辩日期：2026-04-23

---

## 一、项目背景与目标（~1 min）

> "我们用纯 C++17 从零实现一套类 MySQL 语法的数据库管理系统，**不依赖任何外部库**——包括 SQLite、LevelDB 都没有用。用户启动后进入命令行 REPL，输入 SQL，系统完成词法分析、语法分析、执行，把结果以表格形式打印出来。数据持久化到自制的二进制格式文件里。"

---

## 二、团队分工与项目管理（~3 min）

### 角色分工

| 成员 | 角色 | 核心职责 |
|---|---|---|
| **我（PM/架构）** | 项目管理 + 架构师 | 接口设计、CMake 构建、Git 规范、集成测试 |
| 成员B | CLI 工程师 | REPL 主循环、表格渲染、元命令、备份还原 |
| 成员C | 解析层工程师 | Lexer + Parser + AST 全部节点 |
| 成员D | 存储引擎工程师 | 文件读写（.mdb/.tdf/.trd/.tid）+ DDL 执行器 |
| 成员E | 查询执行层工程师 | DML 执行器、WHERE 求值、约束、事务 |

### 为什么这么分？

> "核心原则是**数据流闭环**——每人端到端负责一个链路，而不是按代码类型切（'大家都写一点执行器'），这样避免接口不一致导致集成失败。"

### 工程实践

- **接口先行**：第一天冻结 `types.h` / `AST.h` / `StorageEngine.h` 三个共享头文件，五人并行开发，无需等待彼此
- **Git 工作流**：`main` / `develop` / `feature` 三层分支，feature 提 PR 合并 develop，验收后上 main
- **构建系统**：CMake 管理，`cmake -B build && cmake --build build` 一行出可执行文件

---

## 三、整体架构与构建思路（~3 min）

### 数据流管道

```
用户输入 (stdin)
    │
    ▼
CLI REPL 层        ← Repl.cpp / MetaCommands.cpp / Formatter.cpp
    │ SQL 字符串
    ▼
Lexer              ← 分词，输出 vector<Token>
    │
    ▼
Parser             ← 语法分析，输出 ASTNode*
    │
    ▼
Executor           ← 调度执行，DML/DDL 分发
    │
    ▼
StorageEngine      ← 文件 I/O，读写二进制文件
    │
    ▼
.mdb / .tdf / .trd（磁盘）
```

### 自制二进制格式

| 文件扩展名 | 存储内容 |
|---|---|
| `.mdb` | 数据库系统目录（库名列表）|
| `.tdf` | 表定义文件（字段名/类型/约束）|
| `.trd` | 表记录文件（实际数据行）|
| `.tid` | 索引文件（B+Tree，Week 3）|

### 技术选型理由

> "纯 C++ 标准库，零外部依赖；`std::fstream` 直接控制字节布局；便于脚本化测试：`./dbms < test.sql` 直接批量执行；与课程底层系统编程目标高度契合。"

---

## 四、已实现功能演示（~2 min）

### 当前完成状态（Week 1 全部 + 部分 Week 2）

| 类别 | 已实现 |
|---|---|
| **DDL** | `CREATE/DROP DATABASE`、`CREATE/DROP TABLE`、`SHOW DATABASES/TABLES`、`USE` |
| **DML** | `INSERT`、`SELECT`（含 WHERE）、`UPDATE`、`DELETE` |
| **存储** | `.mdb`、`.tdf`、`.trd` 二进制读写 |
| **CLI** | 多行输入、分号检测、ANSI 彩色、表格渲染 |
| **元命令** | `\help`、`\quit`、`\status` |
| **约束** | PRIMARY KEY、NOT NULL、UNIQUE |

### 演示顺序

```sql
-- 1. 建库建表
CREATE DATABASE school;
USE school;
CREATE TABLE students (id INT PRIMARY KEY, name VARCHAR(50), age INT NOT NULL);

-- 2. 插入数据
INSERT INTO students VALUES (1, 'Alice', 20);
INSERT INTO students VALUES (2, 'Bob', 22);

-- 3. 查询
SELECT * FROM students;
SELECT name, age FROM students WHERE age > 20;

-- 4. 更新删除
UPDATE students SET age = 21 WHERE id = 1;
DELETE FROM students WHERE id = 2;

-- 5. 元命令
\help
\status
```

---

## 五、后续开发计划（~2 min）

| 周次 | 重点 | 关键功能 |
|---|---|---|
| **Week 2**（进行中）| 核心 DML 完善 | ORDER BY、LIMIT、ALTER TABLE、GROUP BY、HAVING、DISTINCT |
| **Week 3** | 高级功能 | B+Tree 索引（`.tid`）、事务（BEGIN/COMMIT/ROLLBACK + undo log）、外键约束、用户权限 GRANT/REVOKE |
| **Week 4** | 测试 & 交付 | 集成测试、性能调优、文档、备份/还原（导出 SQL 文件）|

> "从 commit 历史可以看到，GROUP BY/HAVING/DISTINCT 和索引持久化已有同学在 feature 分支上推进，整体进度符合预期。"

---

## 六、总结一句话

> "中期验收目标是'程序能跑 + 基础 DDL 可执行'，我们提前完成了完整 DML 和 WHERE 求值，为后两周高级功能打好了基础。"

---

## 附：代码快速导读

### 入口文件

- **`src/main.cpp`** — 程序入口，初始化引擎和 REPL，支持批处理模式（`./dbms < script.sql`）

### CLI 层（成员B 负责）

- **`src/cli/Repl.cpp`** — REPL 主循环，读取输入 → 调用引擎 → 打印结果
- **`src/cli/Formatter.cpp`** — 表格渲染 + ANSI 颜色输出
- **`src/cli/MetaCommands.cpp`** — `\help`、`\quit`、`\status` 等元命令处理
- **`src/cli/Session.h`** — CLI 会话状态（当前库、用户）

### 解析层（成员C 负责）

- **`src/engine/lexer/Lexer.cpp`** — 词法分析，把 SQL 字符串切成 Token 流
- **`src/engine/parser/Parser.cpp`** — 语法分析，把 Token 流解析为 AST 树
- **`src/engine/parser/AST.h`** — 所有 AST 节点的类型定义（全局接口文件）

### 执行层（成员E 负责）

- **`src/engine/executor/Executor.cpp`** — 调度执行，根据 AST 节点类型分发 DDL/DML
- **`src/engine/executor/ExprEvaluator.cpp`** — WHERE 条件表达式求值（比较、AND/OR）

### 存储层（成员D 负责）

- **`src/engine/storage/DatabaseManager.cpp`** — 数据库创建/删除，操作 `.mdb` 系统目录
- **`src/engine/storage/TableManager.cpp`** — 表定义读写，操作 `.tdf` 文件
- **`src/engine/storage/RecordManager.cpp`** — 记录行读写，操作 `.trd` 二进制文件
- **`src/engine/storage/IndexManager.cpp`** — 索引管理（`.tid`，Week 3）

### 共享接口

- **`src/types.h`** — 全局类型定义（`QueryResult`、`ColumnType`、`Value` 等），所有模块都依赖此文件
- **`src/engine/DBEngine.h`** — 引擎对外暴露的唯一接口：`execute(sql, session)`

---

*读代码建议：先看 `types.h` 了解数据结构 → `AST.h` 了解语法节点 → `main.cpp` 看启动流程 → 再按数据流顺序阅读各模块*
