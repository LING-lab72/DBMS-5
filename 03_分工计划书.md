# 数据库管理系统（DBMS）分工计划书

**版本**：3.2  
**日期**：2026-04-19  
**项目周期**：4周（Week 1 中期验收，Week 4 正式提交）  
**语言**：纯 C++17，不依赖任何外部库

---

## 1. 团队成员与角色

| 编号 | 代号 | 角色 | 核心职责 |
|---|---|---|---|
| P1 | 成员A | PM / 架构 / 测试工程师 | 项目管理、**共享接口文件审定（types.h / AST.h / StorageEngine.h）**、CMake 构建、代码审查、集成测试、文档 |
| P2 | 成员B | CLI 交互层工程师 | REPL 主循环、终端输出格式化（ANSI）、元命令、批处理模式、**备份/还原** |
| P3 | 成员C | 解析层工程师 | 词法分析器（Lexer）、语法分析器（Parser）**全部语法**（按 A 审定的 AST.h 实现） |
| P4 | 成员D | 存储引擎 + DDL 闭环工程师 | 所有二进制文件读写（.mdb/.tdf/.trd/.tid）、**DDL 执行器**、**安全管理执行** |
| P5 | 成员E | 查询执行层工程师 | DML 执行器、WHERE 求值、完整性约束、索引优化、事务管理、外键约束 |

> **分工原则**：按"数据流闭环"划分——成员D 端到端负责所有 schema 变更（解析由 C 提供 AST，D 接手执行到文件落盘）；成员E 端到端负责所有数据查询与高级语义（DML → 约束检查 → 执行 → 结果集）；成员B 负责所有用户可见的文本输出（终端渲染与 SQL 文件导入导出天然同构）。每人在开发自己模块的同时负责配套单元测试，PM 统一负责跨模块集成测试与最终交付验证。

---

## 2. 分工矩阵（RACI）

> R=负责执行，A=最终负责，C=参与咨询，I=知悉

| 模块 | 成员A(PM) | 成员B(CLI) | 成员C(解析) | 成员D(存储+DDL) | 成员E(查询执行) |
|---|---|---|---|---|---|
| 架构设计 & 接口定义（types.h / AST.h / StorageEngine.h） | R/A | I | C | C | C |
| CMake 构建脚本 & Git 分支规范 | R/A | C | C | I | I |
| Lexer（词法分析器） | I | I | R/A | I | I |
| Parser（语法分析器）& AST 全部节点 | I | I | R/A | C | C |
| 文件管理基础设施（.mdb/.tdf/.trd/.tid） | C | I | I | R/A | I |
| **DDL 执行器**（CREATE/DROP DATABASE/TABLE/INDEX） | I | I | C | R/A | I |
| 系统目录、表定义、记录文件读写 | I | I | I | R/A | I |
| **DML 执行器**（INSERT/SELECT/UPDATE/DELETE） | I | I | C | C | R/A |
| WHERE 条件求值（ExprEvaluator） | I | I | C | I | R/A |
| 完整性约束（PK/NOT NULL/UNIQUE） | I | I | C | C | R/A |
| CLI REPL 主循环 | C | R/A | I | I | I |
| 终端输出格式化（ANSI 颜色 + 表格） & 元命令 | C | R/A | I | I | I |
| **备份（导出 SQL 文件）& 还原（批量执行 SQL）** | C | R/A | I | C | I |
| 索引存储（B+Tree .tid 文件） | I | I | I | R/A | C |
| 索引查询优化（替代全表扫描） | I | I | I | C | R/A |
| 事务管理（BEGIN/COMMIT/ROLLBACK + undo log） | I | I | C | I | R/A |
| FOREIGN KEY 约束 | I | I | C | C | R/A |
| **安全管理**（用户/权限存储 + GRANT/REVOKE 执行） | I | I | C | R/A | I |
| CLI 批处理模式 | C | R/A | I | I | I |
| 集成测试 & 文档 | R/A | C | C | C | C |

---

## 3. 周度任务分解

### Week 1（中期验收）

> **中期验收目标**：`cmake -B build && cmake --build build && ./build/dbms` 可进入 REPL，能执行基本 DDL（建库建表/列表查询），结果在终端以表格形式展示。

| 任务 | 负责人 | 预估工时 | 完成标准 |
|---|---|---|---|
| 初始化 C++17 + CMakeLists.txt，配置 Git 分支规范 | 成员A | 3h | 项目可 `cmake --build build` 编译通过 |
| GitHub 仓库 / 分支策略 / PR 模板 | 成员A | 2h | main/develop/feature 分支规范就绪 |
| **共享接口头文件定义**（`types.h` / `AST.h` 节点骨架 / `StorageEngine.h` 方法签名），经全员确认后冻结 | 成员A | 4h | 三份头文件编译通过，各成员可按接口独立开工，无需再协商 |
| 词法分析器（Lexer）——Token 枚举 + 分词核心实现 | 成员C | 6h | 可对基础 DDL 正确分词，单测通过 |
| 语法分析器（Parser）——DDL 部分（CREATE/DROP DATABASE/TABLE，SHOW，USE） | 成员C | 6h | 输出正确 AST，解析失败有错误信息 |
| **CLI REPL 主循环**（`std::getline` 驱动，支持多行输入与分号检测） | 成员B | 6h | 可输入 SQL，识别语句结束符，回调执行引擎 |
| **终端结果格式化**（自制表格渲染、行数统计、耗时显示） | 成员B | 4h | SELECT 结果以对齐表格输出，DML 输出 affectedRows |
| **终端 ANSI 颜色输出**（错误红色、成功绿色、提示黄色） | 成员B | 2h | 颜色正确渲染，Windows/Linux 均可用 |
| **`\help` / `\quit` / `\status` 元命令** | 成员B | 2h | 各元命令功能正常，不传入引擎解析 |
| 文件管理器 + 系统目录（.mdb）初始化 | 成员D | 3h | 引擎启动时可创建 data 目录与系统文件 |
| 创建/删除数据库（.mdb 存储） + **CREATE/DROP DATABASE 执行器** | 成员D | 5h | 执行 DDL 后目录与元信息文件正确变更 |
| 创建/删除表（.tdf 存储） + **CREATE/DROP TABLE 执行器** | 成员D | 5h | 执行 DDL 后表定义文件正确生成/删除 |
| **数据库/表列表查询执行**（SHOW DATABASES/TABLES，读 .mdb/.tdf） | 成员D | 2h | 返回当前实例的库名/表名列表 |
| 查询执行器框架——调度 AST 节点分发到 D/E 各模块 | 成员E | 4h | 框架编译通过，DDL 分支可路由到成员D接口 |
| **中期验收演示准备** | 成员A | 2h | Demo 流程：启动→建库→建表→查询 通顺 |

---

### Week 2——核心功能

| 任务 | 负责人 | 预估工时 |
|---|---|---|
| Parser 扩展——DML（INSERT/SELECT/UPDATE/DELETE）语法解析 | 成员C | 6h |
| Parser 扩展——WHERE / ORDER BY / LIMIT 子句解析（与成员E约定 WhereExpr AST 接口） | 成员C | 4h |
| 记录文件（.trd）：插入、全表扫描、定点更新、软删除 | 成员D | 10h |
| 字段管理存储——ADD / MODIFY / DROP COLUMN（.tdf 更新） + **ALTER TABLE 执行器** | 成员D | 5h |
| DML 执行器——INSERT / SELECT / UPDATE / DELETE 完整执行 | 成员E | 6h |
| WHERE 条件求值引擎（ExprEvaluator：比较运算 + AND/OR，基于 WhereExpr AST） | 成员E | 4h |
| 完整性约束——PRIMARY KEY / NOT NULL / UNIQUE / DEFAULT 校验 | 成员E | 4h |
| 终端分页浏览（结果超屏时按空格翻页，显示页码/总行数） | 成员B | 4h |
| `\desc <table>` / `\use <db>` / `\tables` / `\databases` 元命令扩展 | 成员B | 3h |
| CLI 历史命令记录（内存环形缓冲，`\history` 命令，上下键复用） | 成员B | 3h |
| Week 2 模块单元测试 | 各自 | 4h |

---

### Week 3——高级功能

| 任务 | 负责人 | 预估工时 |
|---|---|---|
| Parser 扩展——聚合函数（COUNT/SUM/MAX/MIN/AVG）+ CREATE/DROP INDEX + BEGIN/COMMIT/ROLLBACK | 成员C | 8h |
| 索引存储——.tid 文件 B+Tree 实现（节点页读写）+ **CREATE/DROP INDEX 执行器** | 成员D | 6h |
| **安全管理**——用户/权限写入 .mdb + **CREATE USER / GRANT / REVOKE 执行器** | 成员D | 6h |
| 聚合函数执行（COUNT/SUM/MAX/MIN/AVG） + 索引扫描优化（替代全表扫描） | 成员E | 6h |
| 事务管理——BEGIN/COMMIT/ROLLBACK + undo log | 成员E | 6h |
| FOREIGN KEY 约束——参照完整性校验 | 成员E | 4h |
| **数据库备份**（遍历记录生成 SQL 导出文件，复用 Formatter） | 成员B | 4h |
| **数据库还原**（读取 SQL 文件逐条送入引擎，复用批处理模式） | 成员B | 4h |
| Week 3 模块单元测试 | 各自 | 4h |

---

### Week 4——测试 & 交付

| 任务 | 负责人 | 预估工时 |
|---|---|---|
| 集成测试方案制定 + 执行（SQL 脚本驱动） | 成员A | 8h |
| Bug 修复（Week 1~3 遗留问题） | 全员 | 8h |
| CLI 提示信息 / 错误处理完善 | 成员B | 4h |
| README（编译/运行/功能说明 + SQL 语法速查手册） | 成员A | 3h |
| 部署文档（C++ 编译环境配置说明） | 成员A | 2h |
| 代码最终审查 & 合并 main | 成员A | 3h |
| 项目演示 PPT | 成员A + 成员B | 3h |

---

## 4. 协作规范

### 4.1 Git 工作流

```
main          ← 仅接受 PR，通过 Review 才能合并
  └── develop ← 日常集成分支
        ├── feature/cli-repl           (成员B)
        ├── feature/parser-lexer       (成员C)
        ├── feature/storage-ddl        (成员D)
        └── feature/executor-query     (成员E)
```

- 每个功能建独立 feature 分支，完成后提 PR 到 develop
- PR 须至少 1 人 Review 通过方可合并
- commit message 格式：`feat(module): 描述` / `fix(module): 描述`
- **关键接口约定（Week 1 第1天冻结）**：由 A 主导输出 `types.h` / `AST.h` / `StorageEngine.h` 三份接口文件（C 提供 Lexer/Parser 视角的 AST 需求，D 提供存储视角的方法签名需求），A 综合后冻结；此后 C / D / E 按接口独立实现，无需再次跨人协商

### 4.2 每日站会（建议）

每天 5 分钟同步：昨日完成、今日计划、有无阻碍。

### 4.3 代码规范

- C++17 标准（`-std=c++17`），开启 `-Wall -Wextra` 警告
- 头文件统一使用 `#pragma once`
- 类/函数必须有注释说明入参、出参与异常行为
- 异常统一使用自定义 `DBException`（继承 `std::runtime_error`）
- 禁止硬编码路径，数据目录通过构造函数参数传入

### 4.4 验收标准

| 里程碑 | 时间 | 验收内容 |
|---|---|---|
| 中期验收 | Week 1 末 | `cmake --build build` 编译通过，可启动 REPL，能执行基本 DDL，结果在终端输出 |
| 正式提交 | Week 4 末 | 所有 A/B 级功能通过，有文档，可批处理执行 SQL 脚本 |

---

## 5. 个人任务书

> 以下为每位成员按周细化的个人任务清单，作为自我管理与进度核查的依据。

---

### 5.1 成员A —— PM / 测试工程师

| 周次 | 任务 | 预估工时 | 完成标准 |
|---|---|---|---|
| Week 1 | 初始化 C++17 + CMakeLists.txt，配置 Git 分支策略 | 3h | `cmake --build build` 编译通过 |
| Week 1 | 创建 GitHub 仓库，制定分支策略与 PR 模板 | 2h | 分支规范文档就绪 |
| Week 1 | **共享接口头文件定义**（`types.h` 数据类型 / `AST.h` 节点骨架 / `StorageEngine.h` 方法签名），收集 C / D 需求后冻结 | 4h | 三份头文件编译通过，各成员可按接口独立开工 |
| Week 1 | 中期验收演示准备（Demo 脚本 + 编译环境检查） | 2h | Demo 流程通顺 |
| Week 2 | 进度跟进 + 代码 Review（各成员 PR） | 4h | 所有 PR 有审查意见 |
| Week 3 | 代码 Review + 集成联调协调 | 4h | Week 3 功能集成无阻断冲突 |
| Week 4 | 制定并执行集成测试方案（功能/边界/异常用例，SQL 脚本驱动） | 8h | 测试报告完成，缺陷清单输出 |
| Week 4 | 编写 README（编译/运行/功能说明 + SQL 语法速查手册） | 3h | README 可独立指导新用户编译运行 |
| Week 4 | 编写部署文档（C++ 编译环境配置说明） | 2h | 部署文档覆盖 Windows/Linux 常见环境 |
| Week 4 | 代码最终审查 & 合并 main | 3h | main 分支干净，tag 打好 |
| Week 4 | 项目演示 PPT（与成员B合作） | 3h | PPT 包含架构、功能演示、分工说明 |

---

### 5.2 成员B —— CLI 交互层工程师

> **职责说明**：负责所有"用户可见的文本输出"——终端表格渲染与 SQL 文件导出本质上都是将数据格式化为字符串输出；SQL 文件还原则是批量读取并逐条送入引擎，与 `source <file>` 批处理天然同构。两者由同一人实现，不仅内聚性最强，也避免了跨成员重复封装 I/O 逻辑。

| 周次 | 任务 | 预估工时 | 完成标准 |
|---|---|---|---|
| Week 1 | CLI REPL 主循环（`std::getline`，提示符 `dbms> `，多行输入，分号检测） | 6h | 可输入 SQL 并触发引擎调用 |
| Week 1 | 终端结果格式化（自制表格，行数统计，执行耗时） | 4h | SELECT 结果对齐输出，DML 输出受影响行数 |
| Week 1 | ANSI 颜色输出（错误/成功/警告分色，Windows ENABLE_VIRTUAL_TERMINAL_PROCESSING） | 2h | 错误/成功/警告分色显示 |
| Week 1 | `\help` / `\quit` / `\status` 元命令支持 | 2h | 各元命令功能正常 |
| Week 2 | 终端分页浏览（超屏时空格翻页，显示页码/总行数） | 4h | 大结果集可翻页查看 |
| Week 2 | `\desc <table>` / `\use <db>` / `\tables` / `\databases` 元命令扩展 | 3h | 各元命令快捷执行正确 |
| Week 2 | CLI 历史命令记录（内存环形缓冲 + 上下键，`\history` 命令） | 3h | 历史命令可查看并复用 |
| Week 3 | CLI 批处理模式（`./build/dbms < script.sql`，`source <file>` 命令） | 4h | SQL 脚本可非交互执行，结果顺序输出 |
| Week 3 | 数据库备份——遍历所有表记录，生成可重放的 SQL 导出文件 | 4h | 导出文件语法正确，可用于还原 |
| Week 3 | 数据库还原——读取 SQL 文件逐行解析并送入引擎（复用批处理逻辑） | 4h | 还原后数据与原库一致 |
| Week 4 | 提示信息 & 错误处理完善（统一格式，边界情况处理） | 4h | 所有操作均有明确终端反馈 |
| Week 4 | 项目演示 PPT（与成员A合作） | 3h | 截图清晰，演示流程顺畅 |

---

### 5.3 成员C —— 解析层工程师

> **职责说明**：负责整个词法/语法分析管道，职责边界止于 AST 节点输出，不涉及任何执行语义。AST 节点结构（含 WhereExpr、AggregateExpr 等）由 A 在 Week 1 第1天统一定义并冻结于 `AST.h`，C 仅需向 A 提供 Lexer/Parser 视角的节点需求，此后专注于把输入 SQL 正确解析为对应节点，无需与 D / E 协商接口细节。

| 周次 | 任务 | 预估工时 | 完成标准 |
|---|---|---|---|
| Week 1 | 向 A 提供 AST 节点需求（参与接口评审，不负责写 `AST.h`） | 1h | A 收到 C 的节点需求反馈，可写入 `AST.h` |
| Week 1 | 词法分析器（Lexer）——Token 枚举 + 分词核心实现 | 6h | 可对基础 DDL 正确分词，单元测试通过 |
| Week 1 | 语法分析器（Parser）——DDL 部分（CREATE/DROP DATABASE/TABLE，SHOW，USE） | 6h | 输出正确 AST 树，解析失败有明确错误 |
| Week 2 | Parser 扩展——DML（INSERT/SELECT/UPDATE/DELETE）语法解析 | 6h | DML 语句可输出完整 AST |
| Week 2 | Parser 扩展——WHERE / ORDER BY / LIMIT 子句解析 | 4h | 子句 AST 节点正确，符合 `AST.h` 定义 |
| Week 3 | Parser 扩展——聚合函数（COUNT/SUM/MAX/MIN/AVG） | 4h | 聚合查询 AST 节点正确 |
| Week 3 | Parser 扩展——CREATE/DROP INDEX、BEGIN/COMMIT/ROLLBACK、CREATE USER/GRANT/REVOKE | 4h | 上述语句均可正确解析输出 AST |
| Week 4 | SQL 语法兼容性测试协助（覆盖边界 SQL 用例） | 4h | 已知语法用例解析无误 |
| Week 4 | Bug 修复（Parser/Lexer 遗留问题） | 3h | 缺陷清单中解析层问题清零 |

---

### 5.4 成员D —— 存储引擎 + DDL 闭环工程师

> **职责说明**：负责"schema 变更"的完整生命周期——从接收解析层输出的 AST，到执行器逻辑，再到写盘落地，全程不依赖查询语义。`StorageEngine.h` 的方法签名由 D 向 A 提出需求、由 A 统一冻结，D 专注于实现层，不需要与 E 协商接口细节。安全管理的用户/权限元数据同样存储在 `.mdb` 中，与数据库、表定义共享同一套文件接口，由 D 顺带实现相关执行器最为自然。

| 周次 | 任务 | 预估工时 | 完成标准 |
|---|---|---|---|
| Week 1 | 文件管理器 + 系统目录（.mdb）初始化 | 3h | 引擎启动时自动创建 data 目录与系统文件 |
| Week 1 | 数据库存储（.mdb 读写）+ CREATE/DROP DATABASE 执行器 | 5h | 执行 DDL 后目录与元信息文件正确变更 |
| Week 1 | 表定义存储（.tdf 读写）+ CREATE/DROP TABLE 执行器 | 5h | 执行 DDL 后表定义文件正确生成/删除 |
| Week 1 | SHOW DATABASES / SHOW TABLES 执行（读 .mdb/.tdf） | 2h | 返回当前实例下所有库名/表名 |
| Week 2 | 记录文件（.trd）：插入、全表扫描、定点更新、软删除 | 10h | CRUD 后文件字节正确，由 E 调用验证 |
| Week 2 | 字段管理存储（.tdf 更新）+ ALTER TABLE ADD/MODIFY/DROP COLUMN 执行器 | 5h | 字段变更后 .tdf 同步更新，DESCRIBE 返回新结构 |
| Week 3 | 索引文件（.tid）B+Tree 节点页读写 + CREATE/DROP INDEX 执行器 | 6h | B+Tree 读写正确，索引创建/删除后文件变更符合预期 |
| Week 3 | 安全管理：用户/权限写入 .mdb + CREATE USER / GRANT / REVOKE 执行器 | 6h | 可创建用户并分配权限，权限校验可查询 |
| Week 4 | Bug 修复（存储引擎遗留问题） | 4h | 缺陷清单中存储层问题清零 |

---

### 5.5 成员E —— 查询执行层工程师

> **职责说明**：负责"数据查询"的完整语义层——从 AST 到最终 QueryResult，覆盖条件过滤、完整性约束、索引加速与事务保证。E 是接口的纯消费方：AST 节点结构由 A 定义、C 实现，StorageEngine 方法签名由 A 定义、D 实现，E 只需按冻结的接口编写执行逻辑，无需参与任何接口协商，从而将全部精力集中在查询语义的正确性上。

| 周次 | 任务 | 预估工时 | 完成标准 |
|---|---|---|---|
| Week 1 | 查询执行器框架——调度 AST 节点分发到 D（DDL）/ E（DML）各处理函数 | 4h | 框架编译通过，DDL 路由到成员D接口正确 |
| Week 2 | DML 执行器——INSERT / SELECT / UPDATE / DELETE 完整执行 | 6h | DML 端到端结果正确 |
| Week 2 | WHERE 条件求值引擎（ExprEvaluator：比较运算 + AND/OR，按 `AST.h` 中的 WhereExpr 节点实现） | 4h | 过滤条件生效，边界用例通过 |
| Week 2 | 完整性约束——PRIMARY KEY / NOT NULL / UNIQUE / DEFAULT 校验 | 4h | 违反约束时返回明确错误码 |
| Week 3 | 聚合函数执行（COUNT/SUM/MAX/MIN/AVG，与 ORDER BY/LIMIT 联动） | 4h | 聚合查询结果正确 |
| Week 3 | 索引扫描优化——在 WHERE 命中索引字段时调用 IndexManager 替代全表扫描 | 4h | 索引路径执行结果与全表扫描结果一致 |
| Week 3 | 事务管理——BEGIN/COMMIT/ROLLBACK + undo log（记录变更前镜像） | 6h | 事务提交/回滚行为符合预期 |
| Week 3 | FOREIGN KEY 约束——INSERT/UPDATE/DELETE 时参照完整性校验 | 4h | 外键违反时拒绝操作并返回明确错误 |
| Week 4 | Bug 修复（执行器/高级功能遗留问题） | 4h | 缺陷清单中执行层问题清零 |

